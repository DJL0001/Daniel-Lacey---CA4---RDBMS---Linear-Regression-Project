---
title: "R Notebook"
output: html_notebook
---

```{r}
conn = dbConnect(RMySQL::MySQL(),
                             dbname='sql8591402',
                             host='sql8.freesqldatabase.com',
                             port=3306,
                             user='sql8591402',
                             password = rstudioapi::askForPassword("Enter database password"))

```

```{r}

library(DBI)
library(RSQL)
library(RSQLite)
library(dplyr)
library(ymlthis)
library(tidyverse)

conn <- dbConnect(drv = RSQLite::SQLite(), dbname = "daie_ca4_data3.sqlite")
dbListTables(con)
```

```{r}
dbDisconnect(conn)
```